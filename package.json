{
  "name": "cybersec_album_root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix server",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  }
}